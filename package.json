{
  "name": "cytube-playlist-sort-bot",
  "version": "1.0.0",
  "description": "bot which sorts cytube playlist in roundrobin order",
  "scripts": {
    "prestart": "npx tsc",
    "start": "node ./dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.2",
    "typescript": "^5.0.3"
  },
  "dependencies": {
    "@types/node": "^18.15.11",
    "axios": "^1.3.4",
    "socket.io-client": "^4.6.1"
  }
}
